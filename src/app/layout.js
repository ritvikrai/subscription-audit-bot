import './globals.css'
export const metadata = { title: 'Subscription Audit', description: 'Find and cancel wasteful subscriptions' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
